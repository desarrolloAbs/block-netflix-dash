<?php
class block_netflix_dash extends block_base {

public function  init() {
$this->title = get_string('pluginname' , 'block_netflix_dash');
}
 public function get_content () {
global $BD, $CFG, $OUTPUT;

if ($this->content !==  null) {
return $this->content;
}
$this->content = new stdClass();

//1. Obtener los recursos (excluyendo el curso principal de la portada 'site')
$courses = $DB->get_records_select('course' , 'id != 1', null, 'sortorder ASC');

